//JavaScript function for overlay component

function openNav() {
  document.getElementById("nav-primary").style.height = "100%";
}

/* Close when someone clicks on the "x" symbol inside the overlay */
function closeNav() {
  document.getElementById("nav-primary").style.height = "0%";
}
